<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:BingeBuddy.ViewModels"
    x:Class="BingeBuddy.Pages.HomePage"
    BackgroundColor="#2C3E50"
    NavigationPage.HasNavigationBar="False">

    <ContentPage.BindingContext>
        <viewmodels:MovieViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto,Auto,Auto,*" Padding="10">
        <!-- Top bar -->
        <Grid Grid.Row="0" BackgroundColor="#2C3E50" Padding="10" HeightRequest="60">
            <Image Source="logo.png" HeightRequest="40" HorizontalOptions="Start" />
            <SearchBar Placeholder="Search" HorizontalOptions="FillAndExpand" Text="{Binding SearchText}"/>
        </Grid>

        <Label Grid.Row="1"
               Text="WATCHLIST"
               FontSize="24"
               FontAttributes="Bold"
               TextColor="White"
               HorizontalOptions="Center" />

        <!-- Status Filter -->
        <Grid Grid.Row="2" Padding="10" ColumnDefinitions="*,*" ColumnSpacing="10">
            <Button Grid.Column="0"
                    Text="In Progress"
                    Command="{Binding ShowInProgressCommand}"
                    BackgroundColor="{Binding InProgressButtonColor}"
                    TextColor="{Binding InProgressTextColor}"
                    Padding="5"
                    CornerRadius="5"
                    HorizontalOptions="FillAndExpand"/>
            <Button Grid.Column="1"
                    Text="Completed"
                    Command="{Binding ShowCompletedCommand}"
                    BackgroundColor="{Binding CompletedButtonColor}"
                    TextColor="{Binding CompletedTextColor}"
                    Padding="5"
                    CornerRadius="5"
                    HorizontalOptions="FillAndExpand"/>
        </Grid>

        <!-- Movie Collection (scrollable) -->
        <CollectionView Grid.Row="3"
                        ItemsSource="{Binding FilteredMovies}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="2" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border BackgroundColor="#1F2A38" StrokeShape="RoundRectangle" StrokeThickness="1" Margin="15,5" Padding="10">
                        <VerticalStackLayout Spacing="10" Padding="10">
                            <Image Source="{Binding CoverPhoto}"   
                                   HeightRequest="150"   
                                   Aspect="AspectFill"   
                                   HorizontalOptions="Center" />
                            <Label Text="{Binding Title}"   
                                   FontSize="18"   
                                   FontAttributes="Bold"   
                                   TextColor="White"   
                                   HorizontalOptions="Center" />
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
